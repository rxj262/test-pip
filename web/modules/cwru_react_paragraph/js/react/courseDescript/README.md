#Course Description

## Pulls WSOM course information for display

Version may have issues parsing names from search bar.

When using with Drupal, App.js must be omitted. <CourseDescription /> component should be imported and attached directly to index.js. Run "npm run build" to create production build, then proceed to follow "react paragraph" markdown instructions.
________________________________________________________________________________________________________________________________________________________________
